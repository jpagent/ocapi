# docker-compose.yml
version: "3.8"

services:
  opencode:
    build: .
    ports:
      - "4097:4097"
    environment:
      - OPENCODE_MODEL=x-ai/grok-3-fast
    volumes:
      - ./workspace:/workspace
    restart: unless-stopped
